let name = "Ilya";

alert( `hello ${1}` ); // ? = I think this result is just 1

alert( `hello ${"name"}` ); // ? = I think this result is just name

alert( `hello ${name}` ); // ? = I think this result is Ilya